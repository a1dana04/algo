/**
 * @param {string} num1
 * @param {string} num2
 * @return {string}
 */
var multiply = function(num1, num2) {
       let res3 = BigInt(num1);
  let res4 = BigInt(num2);
  let res5 = res3 * res4;
  return res5 + "";
    
};